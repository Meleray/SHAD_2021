#include <bits/stdc++.h>

using namespace std;

void biuld() {

}

int main() {
    int n;
    cin >> n;
    vector<int> a(n);
    vector<int> b(n);
    for (int i = 0; i < n; ++i) {
        cin >> a[i];
    }
    for (int i = 0; i < n; ++i) {
        cin >> b[i];
    }
    vector<int> ta;
    vector<int> tb;
    
    for (int j = i + 1; j < )
    cout << ans  << endl;
}